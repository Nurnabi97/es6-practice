const names = [
    {id: 24, name: "nurnabi"},
    {id: 39, name: "rakib"}

];
const catchName = [33];
for(let i = 0; i = names.length; i++){
    
    const element = names[i];
    // const element = element + 1;

    // catchName.push(element);
   

}
let result = names.map((e) => ([0]));
console.log(result);